from enes100 import enes100
enes100.begin("Blaze Busters","FIRE",67,1120)

while (Enes100.isVisible()):
  if (Enes100.getY() > 1):
    #OTV is in top section of zone 1
    while (Enes100.getTheta() != -1.57):
      # turn left .1 theta
  else:
    #OTV is in bottom section of zone 1
    while (Enes100.getTheta()< 1.55 && Enes100.getTheta()> 1.55 ):


